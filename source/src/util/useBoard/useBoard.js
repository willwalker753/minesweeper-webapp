import { useState, useMemo } from 'react';

/*
this hook translates reusable minesweeper classes into a contract usable by react...
in order to keep logic independant from react
*/
const useBoard = ({

}) => {
    const [cellValues, setCellValues] = useState([]);
    
    return [

    ]
}

export default useBoard;